from setuptools import setup, find_packages

setup(
    name='CelestialVault',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # 在此处列出你的依赖项
    ],
    entry_points={
        'console_scripts': [
            # 在此处定义你的命令行脚本
        ],
    },
)
